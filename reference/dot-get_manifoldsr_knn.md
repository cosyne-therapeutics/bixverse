# Helper function to generate manifoldsR nearest neighbours

Helper function to generate manifoldsR nearest neighbours

## Usage

``` r
.get_manifoldsr_knn(x)
```

## Arguments

- x:

  `SingleCells` object from which to extract the kNN data

## Value

The manifoldsR `NearestNeigbours` if possible
