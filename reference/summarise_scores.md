# Summarise gene scores if they are duplicates.

Summarise gene scores if they are duplicates.

## Usage

``` r
summarise_scores(x, summarisation = c("max", "mean", "harmonic_sum"))
```

## Arguments

- x:

  Named numeric.

- summarisation:

  String. Which summary function to use.

## Value

Named numeric.
