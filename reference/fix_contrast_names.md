# Fixes contrast names for DGEs

Fixes contrast names for DGEs

## Usage

``` r
fix_contrast_names(x)
```

## Arguments

- x:

  Vector of strings or factors.

## Value

Vector with fixed naming based on R conventions.
