# Get the file path to the counts in cell format

Get the file path to the counts in cell format

## Usage

``` r
get_rust_count_cell_f_path(object)
```

## Arguments

- object:

  `SingleCells` class.

## Value

The path to the `counts_cells.bin`
