# Helper function to calculate the multi-level error

Helper function to calculate the multi-level error

## Usage

``` r
multilevel_error(pval, sample_size)
```

## Arguments

- pval:

  Numerical vector. The p-values.

- sample_size:

  Integer. The sample size.

## Value

Returns the log2error
