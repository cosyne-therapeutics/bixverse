# Create distance object from a vector

Create distance object from a vector

## Usage

``` r
create_dist_obj(x, size)
```

## Arguments

- x:

  Numerical vector. The upper-triangle values for which to generate the
  `dist` object.

- size:

  Integer. Nrow (or ncol) of the symmetric matrix.

## Value

Returns the distance object
